"""
crystal_recon — 3D crystal reconstruction pipeline package.

Modules:
    config          — Central configuration settings
    image_utils     — Image loading, resizing, and display
    segmentation    — SAM and OpenCV crystal segmentation
    reconstruction  — 3D geometry, voxel carving, scale calibration
    mesh            — Point cloud to surface mesh pipeline
"""
